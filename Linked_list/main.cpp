#include <iostream>
#include <vector>
#include <string>

using namespace std;

// LL, is to avoid extra space and time complexity where sequence containers
// double up the capacity

// Hash Maps aren't ordered, LL solves the this problem.

// HEAD -> (DATA, NEXT)-> (DATA, NEXT) -> TAIL -> NULL

/*
 * TIME COMPLEXITY
 * prepend O(1)
 * append O(1)
 * lookup O(n)
 * insert O(n)
 * delete O(n)
*/

int main() {
    
    return 0;
}